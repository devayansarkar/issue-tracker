{
  "name": "issue-tracker-e2e-tests",
  "version": "1.0.0",
  "description": "Integration tests for Issue Tracker using Cypress.io.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:run:record": "cypress run --record --key"
  },
  "keywords": [
    "Cypress",
    "cypress.io",
    "integration",
    "tests"
  ],
  "author": "Devayan Kumar Sarkar",
  "license": "ISC",
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.0.0",
    "cypress": "^8.7.0",
    "cypress-dotenv": "^2.0.0",
    "dotenv": "^10.0.0"
  }
}
